--- #my loop playbook for testing perpose
- hosts: demo
  user: ansible
  become: yes
  connection: ssh
  tasks:
    - name: add list of users in my nodes
      user: name="{{item}}" state=present
      with_items:
        - sachinraj
        - bhupendra
        - rajat
        - vasco D gama
